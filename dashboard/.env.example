# Copy to .env.development.local for per-developer overrides, or set
# VITE_API_URL at build time for production deploys.
#
# Only VITE_-prefixed variables get exposed to client-side code.
VITE_API_URL=http://localhost:8000
